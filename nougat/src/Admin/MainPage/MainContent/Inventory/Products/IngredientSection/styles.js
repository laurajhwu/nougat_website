import styled from "styled-components";
import { AddToQueue } from "@styled-icons/boxicons-regular";

export const Add = styled(AddToQueue)`
  display: flex;
  margin: 0 10px 0 auto;
  width: 25px;
  &:hover {
    cursor: pointer;
  }
`;

import styled from "styled-components";
import { Modal, Button, Form, Col } from "react-bootstrap";

export const Container = styled.div`
  display: flex;
  flex-direction: row;
`;

export const Img = styled.img`
  width: 50px;
`;

export const File = styled(Form.File)``;
